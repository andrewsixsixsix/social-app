{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "noEmitOnError": true,
    "baseUrl": "src",
    "paths": {
      "@components": ["components"],
      "@constants": ["constants"],
      "@styles": ["styles"],
      "@types": ["types"],
      "@utils": ["utils"]
    }
  },
  "exclude": ["node_modules"]
}
