{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "noEmitOnError": true,
    "baseUrl": "src",
    "paths": {
      "@components": ["components"],
      "@constants": ["constants"],
      "@styles": ["styles"]
    }
  },
  "exclude": ["node_modules"]
}
